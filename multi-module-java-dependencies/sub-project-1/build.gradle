task('goodbye').doLast {
    println "bye from $project.name"
}

task('task-1').doLast {
    println("task - 1: ${project.name}")
}

println("$project.name configuration phase from sub-project-1/build.gradle")